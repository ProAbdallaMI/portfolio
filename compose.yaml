
services:
  portfolio:
    image: ghcr.io/proabdallami/portfolio:latest
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "127.0.0.1:80:80"
    networks:
      - portfolio

networks:
  portfolio:
    name  : portfolio
    driver: bridge
